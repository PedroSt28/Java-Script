/*Operações ternarias

Ultilização : um contexto com apenas dois caminhos 
exemplo: header com ou sem foto de usuario
*/

const notaFinal = 6
const statusAluno = notaFinal >= 3 ? 'aprovado' : 'reprovado'

console.log(statusAluno)